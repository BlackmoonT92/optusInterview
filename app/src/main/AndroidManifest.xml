<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tam.com.interviewoptus"
    >
  <uses-permission android:name="android.permission.INTERNET"/>


  <application
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/AppTheme"
      android:name=".app.App"
      >
    <activity android:name=".screen.home.MainActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>

        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>

    <activity
        android:name=".screen.scenario1.Scenario1Activity"
        android:configChanges="orientation|screenSize"
        android:label="@string/scenario1"
        />
    <activity
        android:name=".screen.scenario2.Scenario2Activity"
        android:configChanges="orientation|screenSize"
        android:label="@string/scenario2"
        ></activity>
  </application>

</manifest>